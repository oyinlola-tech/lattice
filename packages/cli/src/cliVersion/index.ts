/**
 * @lattice/cli — CLI Version
 *
 * Semantic version parsing, comparison, and formatting.
 */

export {
  getCLIVersion,
  formatCLIVersion,
  getVersionString,
  isValidVersion,
  compareVersions,
  parseVersion,
  isCompatibleVersion,
  type CLIVersionInfo,
} from "./cliVersion.core.js";
