/**
 * @zudolib/lifecycle/lifecyclePhase
 *
 * Lifecycle phase enum.
 */

/**
 * Lifecycle phases that components can participate in.
 */
export enum LifecyclePhase {
  Initialize = "initialize",
  Start = "start",
  Ready = "ready",
  Stop = "stop",
  Dispose = "dispose",
}
