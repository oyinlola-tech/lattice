/**
 * Queue registry for queue instances.
 *
 * Provides registration and lookup of queue instances.
 */
export { createQueueRegistry } from "./queueRegistry.core.js";

export type { QueueRegistry, QueueInfo } from "./queueRegistry.type.js";
