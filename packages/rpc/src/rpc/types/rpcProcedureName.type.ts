/**
 * @lattice/rpc/types
 *
 * Core types for the Lattice RPC package.
 */

/**
 * A valid RPC procedure name.
 *
 * Must be dot-separated lowercase identifiers, e.g. "users.getUser".
 */
export type RPCProcedureName = string;
