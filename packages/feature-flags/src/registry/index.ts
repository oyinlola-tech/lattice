/**
 * Feature flag registry — central store for flag definitions.
 *
 * @module registry
 */

export { createFeatureFlagRegistry } from "./registry.core.js";
export type { FeatureFlagRegistry } from "./registry.core.js";
