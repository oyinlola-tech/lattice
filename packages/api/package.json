{
  "name": "@lattice/api",
  "version": "0.1.0",
  "private": true,
  "description": "Application-facing API layer for Lattice — operation definitions, execution context, interceptors, policies, and transport-agnostic contracts.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@lattice/errors": "0.1.0",
    "@lattice/constants": "0.1.0",
    "@lattice/types": "0.1.0",
    "@lattice/schema": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
